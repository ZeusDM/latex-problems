\documentclass[10pt, a4paper]{article}
\usepackage[utf8]{inputenc}
\usepackage[brazilian]{babel}
\usepackage{lmodern}
\usepackage[left=2.5cm, right=2.5cm, top=2.5cm, bottom=2.5cm]{geometry}

\usepackage{zeus}

\title{Álgebra}
\author{Andrey}
\mail{}
\titlel{Treinamento IMO}
\titler{}

\begin{document}	
	\zeustitle
	\defn{$\alpha$ e $\beta$ são conjudados quando, se $P \in \QQ[x]$, então $P(\alpha) = 0 \iff P(\beta) = 0$.}
	\defn{Seja $I_\alpha = \{ P \in \QQ[x] : P(\alpha) = 0\}$. O elemento mônico de menor grau em $I_\alpha$ é o polinômio minimal de $\alpha$, $P_\alpha$.}
	\defn{Podemos redefinir $\alpha$ e $\beta$ conjugados como $P_\alpha(\beta) = 0$.}
	\lem{$P_\alpha$ é irredutível.}
	%demonstração
	\lem{$Q(\alpha) = 0$ e $Q$ irredutível $\implies Q = c \cdot P_\alpha$.}
	%demosntração
	\defn{$P \in \QQ[x_1, x_2, \dots, x_n]$ é simétrico se $P(x_1, x_2, \dots, x_n) = P(x_{\pi(1)}, x_{\pi(2)}, \dots, x_{\pi(n)})$, para toda permutação $\pi$ de $\{1, 2, \dots, n\}$.}
	\thm{$x_1, x_2, \dots, x_n$. $\sigma_k := \sum_{k elem} \prod x_i$. $P$ é simétrico $\iff$ existe $Q \in \QQ[y_1, y_2, \dots, y_n]$  $P(x_1, x_2, \dots, x_n) = Q(\sigma_1, \sigma_2, \dots, \sigma_n)$.}
	\cor{$\alpha_1, \alpha_2, \dots, \alpha_n$ são conjugados, então $\sigma_i \in \QQ$.}
	\thm{$\alpha$ e $\beta$ são algébricos, então $\alpha + \beta$, $\alpha\beta$, $\alpha^{-1}$ e $\alpha^2$ são algébricos.}\par
	Tem muitos jeitos de provar.
	\defn{(Extensão de Corpo) } %definir extensão de corpo 
	\defn{(Grau de uma Extensão) Dimensão de $K$, visto como espaço vetorial em $\RR$}
	\problem{math/brazil/mo/2017/6}
	\thm{$\Phi_n$ é irredutível.}
	\lem{$\Phi_p$ é irredutível.}
	\lem{(Critério de Eisenstein)}
	\lem{$\Phi_{2^n}$ é irredutível.}
	\proof{Vamos mostrar que, se $\omega$ é raiz de $x^{2^{n-1}} + 1$, então $P_{\omega} = P_{\omega^3} = P_{\omega^5} = P_{\omega^7}.$}
	\lem{Seja $a \in \ZZ_{2^n}^{\times}$, existe $m$ tal que $a \equiv 3^m$ ou $a \equiv 5^m$ ou $a \equiv 7^m$.}
	\prob{Seja $f \in \QQ[x]$ não constante. Se $\alpha^3 - 3\alpha + 1 = f(\alpha)^3 - 3f(\alpha) + 1 = 0$. Prove que $(f^n(\alpha))^3 - 3f^n(\alpha) + 1 = 0$.}
	\prob{Seja $n$ ímpar e $a_1, a_2, \dots, a_n \in \ZZ$ distintos. Prove que $(x-a_1)(x-a_2)\cdots(x-a_n) - 1$ é irredutível em $\QQ[x]$.}
	\lem{(Lema de Gauss)}
	\problem{math/imo/1993/1}
	\prob{Existe $P \in \ZZ[x]$, irredutível em $\ZZ[x]$, mas redutível em $\ZZ_q[x]$, para todo $q$ primo.}
	\begin{sol}
		{$P(x) = x^4 - 10x^2 + 1$.}
	\end{sol}
	\prob{Prove que, para todo $n$, existe $f \in \ZZ_p[x]$ irredutível tal que o grau de $f$ é $n$.}
	\prob{Prove que, se $d$ divide $a^4 + a^3 + 2a^2 - 4a + 3$, então $d \equiv x^4 \bmod{13}$.}
\end{document}
